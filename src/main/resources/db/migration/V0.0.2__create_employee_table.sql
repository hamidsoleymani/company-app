create TABLE employee
(
    id            int         NOT NULL,
    employee_name varchar(80) NOT NULL,
    age           int         NOT NULL,
    email         varchar(80),
    company_id int not null
);
